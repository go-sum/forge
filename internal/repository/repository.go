// Package repository provides the data-access layer for application-specific
// storage operations (health checks, future app tables). User management is
// handled entirely by pkg/auth/pgstore.
package repository
