+++
title = "security"
description = "Reusable HTTP security primitives for Go web applications."
weight = 10
+++

Reusable HTTP security primitives for Go web applications.

This package is framework-agnostic. Echo wiring and app-specific responses stay
in the consuming application's `internal/` tree.
